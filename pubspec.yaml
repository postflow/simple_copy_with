name: copy_with_annotations
description: generates a copywith method for classes marked @copywith annotation using build_runner
homepage: https://github.com/postflow/simple_copy_with
version: 0.0.2


environment:
  sdk: ">=2.14.0 <3.0.0"
